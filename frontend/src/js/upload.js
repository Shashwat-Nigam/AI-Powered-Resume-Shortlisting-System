const BASE_URL = 'http://localhost:8080/api'; document.getElementById('uploadForm').addEventListener('submit', async (e) => { e.preventDefault(); const formData = new FormData(); const files = document.getElementById('resumeFiles').files; for (let file of files) formData.append('files', file); const response = await fetch(`${BASE_URL}/resumes/upload`, { method: 'POST', body: formData }); const candidates = await response.json(); const tableBody = document.getElementById('tableBody'); tableBody.innerHTML = candidates.map(c => `<tr><td class='p-2 border'>${c.name}</td><td class='p-2 border'>${c.score}%</td><td class='p-2 border'>${c.status}</td></tr>`).join(''); document.getElementById('resultTable').classList.remove('hidden'); });